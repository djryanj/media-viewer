/**
 * Feature flags — enable/disable incomplete or experimental features.
 * Set a flag to `true` to expose the feature in the UI.
 */
export const features = {
    /** Albums — the standalone Albums browse page; not yet implemented. */
    albums: false
};
